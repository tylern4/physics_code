name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: mkbuild
      run: cd current && mkdir build && cd build
    - name: cmake
      run: cmake --version && cmake ..
    - name: make
      run: make
