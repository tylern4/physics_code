name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: docker build
      run: |
        docker run \
        -v ${{ github.workspace }}:/tmp \
        uofscphysics/root /bin/bash -c "cd /tmp && ls -l"
