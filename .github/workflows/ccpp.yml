name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: mkbuild
      run: pwd && ls -l && mkdir build && cd build
    - name: cmake
      run: cmake ../current
    - name: make
      run: make
